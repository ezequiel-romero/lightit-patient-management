import { Button } from './'

export const Header = () => {
  return (
    <header>
      <img src="logo.svg" alt="logo" height="32" />
      <h1>Patient Data Management</h1>
      <Button text="Sign up" />
    </header>
  )
}
